<?php
/**
 * 회사 정보 — 사이트 전체에서 쓰는 값을 여기 한 곳에 모읍니다.
 *
 * ⚠️ 여기만 고치면 푸터·모바일 하단바·문의 페이지에 한 번에 반영됩니다.
 *    다른 파일은 손대지 마세요.
 */

if ( ! defined( 'ABSPATH' ) ) {
	exit;
}

function caparol_info( $key = '' ) {

	$info = array(

		/* ── 여기부터 실제 값으로 바꿔주세요 ──────────────── */

		'company'   => '카파롤코리아',
		'ceo'       => '',                       // 대표자명
		'biz_no'    => '',                       // 사업자등록번호  예: 123-45-67890
		'address'   => '',                       // 주소
		'tel'       => '02-000-0000',            // 대표전화 (화면 표시용)
		'fax'       => '',
		'email'     => '',                       // 대표 이메일
		'hours'     => '평일 09:00 – 18:00 (점심 12:00 – 13:00)',
		'kakao'     => '',                       // 카카오톡 채널 URL

		// 문의 폼이 도착할 메일 주소. 비워두면 워드프레스 관리자 메일로 갑니다.
		'inquiry_to' => '',

		/* ── 여기까지 ─────────────────────────────────────── */

		'tagline'   => '독일 Caparol 제품을 직접 수입·공급하는 한국 공식 법인입니다.',

		/* ── 홈 화면 문구 ─────────────────────────────────
		   히어로(첫 화면)에 크게 나오는 글입니다. */
		'hero_lead'  => '독일 Caparol 한국 공식 수입원',
		'hero_title' => "130년 독일 기술의\n건축용 도료 · 마감재",
		'hero_text'  => '프라이머부터 외단열시스템까지, 현장에서 검증된 Caparol 제품을 카파롤코리아가 직접 수입해 공급합니다.',

		/* ── 홈 화면 숫자 4개 ─────────────────────────────
		   ⚠️ 아래 값은 확인 가능한 사실만 적혀 있습니다.
		      국내 시공 건수·취급 색상 수 같은 수치를 넣고 싶으시면
		      실제 숫자를 확인한 뒤 바꿔주세요. 근거 없는 숫자는 신뢰를 깎습니다. */
		'stats' => array(
			array( '1895',   '독일 Caparol 창립' ),
			array( '130년',  '축적된 도료 기술' ),
			array( 'DAW',    '독일 본사 그룹' ),
			array( '공식',   '한국 수입·공급' ),
		),
	);

	// 전화번호에서 하이픈을 뺀 값 — tel: 링크용
	$info['tel_raw'] = preg_replace( '/[^0-9+]/', '', $info['tel'] );

	if ( '' === $key ) {
		return $info;
	}

	return isset( $info[ $key ] ) ? $info[ $key ] : '';
}
